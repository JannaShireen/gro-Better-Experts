import 'package:flutter/material.dart';

class UserThoughts extends StatelessWidget {
  const UserThoughts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Thoughts'),
    );
  }
}
